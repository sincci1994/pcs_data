# === Airflow 메인 환경에 설치되는 패키지 ===
# (dbt-oracle 는 의존성 충돌 회피를 위해 Dockerfile에서 별도 venv에 설치한다)

# Oracle 연결 (thin 모드 → Instant Client 불필요)
apache-airflow-providers-oracle==3.10.0
oracledb==2.4.1

# SqlSensor 등 공통 SQL 오퍼레이터/센서
apache-airflow-providers-common-sql==1.14.0

# dbt 모델을 Airflow Task로 자동 렌더링 (.sql 추가 → Task 자동 생성)
astronomer-cosmos==1.5.1

# OpenLineage → Marquez (데이터 플로우/런타임 헬스). Cosmos가 dbt artifacts를 파싱해 emit
apache-airflow-providers-openlineage==1.10.0
openlineage-integration-common==1.20.5

# DagFactory(YAML) 파싱
PyYAML==6.0.2

# 목데이터/검증 보조
pandas==2.2.2
