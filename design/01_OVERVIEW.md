# 01. 개요

## 문제
- 레거시 앱 VIEW_TABLE 불신 → 엔지니어별 Excel 재가공 → **개인 판단이 만든 불규칙한 그림자 데이터 프로덕트**.
- 엔지니어는 담당 영역만 알고, **전체 비즈니스 플로우 문서가 없다** — VOC 패치로만 변형, 합의 사항 방치.
- 결과: 같은 지표가 담당자마다 다른 값, 재현·검증 불가.

## 목적
1. **표준화** — 실무 담당자의 변환 논리를 발굴해 dbt 모델(SQL)로 단일화, LND→SLV→GOLD 계층에 정착.
2. **가시화** — 비즈니스 정의(용어집)와 계보(리니지)로 현재 데이터 흐름을 본다 (팔란티어 온톨로지 지향).
3. **셀프서비스** — 도메인 지식이 높은 분석가가 SQL·정의서를 직접 추가하면 파이프라인에 자동 반영.
4. **팀 역량** — 위 과정 자체가 PCS기술팀의 업무 능력·데이터 활용능력 향상 수단.

## 접근
| 단계 | 내용 | 성격 |
|---|---|---|
| 발굴 | 인터뷰·수집으로 Excel 변환 논리를 SQL로 이관, 레거시 산출물과 대사 검증 | **한시적** → [04_AS_IS_INTAKE.md](04_AS_IS_INTAKE.md) |
| 정착 | dbt 모델 + Manager/Model DAG + OpenMetadata 카탈로그 | 상시 운영 |
| 확장 | 분석가가 모델을 자유롭게 추가·조회 (AI Agent 병용) | 상시 운영 |

## 이해관계자
PCS기술팀 엔지니어(=분석가·도메인 전문가) · 시스템 담당 · 품질 담당.

## 북극성
- 저작(모델·정의서 작성)은 Agent와 사람이 자유롭게, **실행(Airflow 파싱·스케줄)은 결정론적으로** — 런타임 LLM 금지.
