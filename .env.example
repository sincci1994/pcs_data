# 로컬 실습 스택 — .env 없이도 기본값으로 기동된다. 값을 바꾸려면 이 파일을 .env 로 복사 (커밋 금지).
PCS_WH_ADMIN_PASSWORD=pcs_admin
PCS_DBT_USER=dbt_exec
PCS_DBT_PASSWORD=dbt_pass

# 사내 빌드 환경 (집/일반망은 비워두면 no-op) — 이미지 빌드 시점에만 적용
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,warehouse
PIP_INDEX_URL=
PIP_EXTRA_INDEX_URL=
