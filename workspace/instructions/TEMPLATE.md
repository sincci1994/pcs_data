# 지시서 — <작업명>

<!-- 사용법: 이 파일을 <작업명>.md 로 복사해 채운 뒤 Agent에게 "이 지시서를 수행하라"고 지시한다.
     4절 전부 필수. 트랙별로 뭘 쓰면 되는지는 GUIDE.md 트랙 0 표 참조. -->

## 목표
<!-- 한 문단. 왜 하는지 + 무엇이 되면 성공인지. 수용 기준이 있으면 여기 명시. -->

## 범위
<!-- Agent가 만질 디렉토리·파일을 명시 (예: platform/extract/sources.yml 블록 추가).
     "범위 밖" 항목도 적으면 스코프 표류를 막는다. -->

## 제약
<!-- 지켜야 할 불변 규칙 중 이 작업에 걸리는 것만 골라 링크한다.
     예: 시크릿은 .env만(커밋 금지) / 정의 선행(governance 먼저) / 런타임 LLM 금지 /
         분석가 셀프서비스(모델 추가에 개발자 코드 금지) — 근거 설계 문서 링크 필수. -->

## 완료 조건
<!-- 검증 가능한 문장으로 번호를 매긴다 — "확인한다"가 아니라 "X가 Y이다".
     예: 1. Airflow에 extract__<소스> DAG가 생성되고 수동 트리거가 success.
         2. brz.<타깃> 건수 = 소스 필터 건수. 3. 재실행 2회에도 건수 불변(멱등). -->
