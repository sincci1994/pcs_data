# 컬럼 설명은 모델 .sql 헤더 주석에 — 이 파일은 테스트 정의만.
version: 2

models:
  - name: stg_smdm__eqp_org_mapping
    columns:
      - name: eqp_id
        tests: [unique, not_null]
      - name: team_code
        tests: [not_null]
      - name: division_code
        tests: [not_null]
      - name: site_code
        tests: [not_null]
      - name: line_code
        tests: [not_null]
      - name: area_code
        tests: [not_null]
