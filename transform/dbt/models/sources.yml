version: 2

sources:
  - name: lnd
    schema: PCS_LND       # dbt-oracle: source 는 schema.table 로 해석
    description: "수신 버퍼 — Airflow 추출 DAG 이 적재"
    tables:
      - name: L_SCADA_TRACE
      - name: L_SCADA_EQP
      - name: L_SCADA_SENSOR
